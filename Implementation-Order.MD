# Implementation Order - تطبيق تكة

## 1. الهدف من الوثيقة
هذه الوثيقة تحدد ترتيب التنفيذ العملي للمشروع خطوة بخطوة، بحيث نبدأ بالأجزاء المؤسسة ثم نبني فوقها بدون ارتباك أو إعادة عمل.

---

## 2. مبدأ التنفيذ
الترتيب المقترح يعتمد على:

- بناء الأساس أولًا
- تقليل الاعتماد المتبادل المعقد
- تشغيل رحلة كاملة مبكرًا
- الوصول إلى `MVP` بأسرع شكل منظم

---

## 3. الترتيب التنفيذي المقترح

### المرحلة 1: تهيئة المشروع
#### الهدف
إعداد البيئة الأساسية للتطوير.

#### التنفيذ
1. إنشاء مشروع `Flutter` موحد للعميل والمطبخ
2. إنشاء مشروع `Next.js` للإدارة
3. إعداد المستودع وهيكل المجلدات
4. ضبط ملفات البيئة `env`

#### الناتج
- بيئة جاهزة للبناء

---

### المرحلة 2: إعداد قاعدة البيانات والبنية الخلفية
#### الهدف
بناء الأساس الذي ستعتمد عليه كل الشاشات والعمليات.

#### التنفيذ
1. إنشاء الجداول الأساسية:
   - `users`
   - `regions`
   - `customer_addresses`
   - `kitchens`
   - `kitchen_documents`
   - `kitchen_payment_methods`
   - `menu_items`
   - `menu_item_sizes`
   - `orders`
   - `order_items`
   - `deposit_proofs`
   - `order_messages`
   - `reviews`
2. إعداد العلاقات والفهارس
3. إعداد `Row Level Security`
4. إعداد `Storage Buckets`

#### الناتج
- قاعدة بيانات قابلة للاستخدام

---

### المرحلة 3: المصادقة والأدوار
#### الهدف
تأمين الدخول وفصل صلاحيات العميل والمطبخ والإدارة.

#### التنفيذ
1. تسجيل الدخول وإنشاء الحساب من تطبيق موحد
2. اختيار الدور عند التسجيل أو الدخول: `customer` أو `kitchen_owner`
3. تسجيل دخول الإدارة
4. بناء طبقة الجلسات
5. ربط كل دور بشاشاته وصلاحياته

#### الناتج
- دخول وخروج وصلاحيات مستقرة

---

### المرحلة 4: تسجيل المطبخ والاعتماد
#### الهدف
تشغيل أول دورة اعتماد حقيقية للمطابخ.

#### التنفيذ
1. بناء شاشة استكمال ملف المطبخ
2. رفع اللوجو والصور
3. رفع صورة البطاقة
4. حفظ بيانات `InstaPay`
5. بناء شاشة انتظار الاعتماد
6. بناء شاشات الإدارة لاعتماد المطابخ

#### الناتج
- دورة اعتماد مكتملة من التسجيل حتى الموافقة

---

### المرحلة 5: ملف المطبخ والمنيو
#### الهدف
تمكين المطابخ المعتمدة من عرض نفسها ووجباتها.

#### التنفيذ
1. بناء صفحة ملف المطبخ
2. بناء إدارة الصور
3. بناء إدارة المنيو
4. دعم الأحجام
5. فتح/غلق المطبخ يدويًا

#### الناتج
- مطبخ جاهز للظهور والطلب

---

### المرحلة 6: العناوين والموقع واكتشاف المطابخ
#### الهدف
تمكين العميل من الوصول للمطابخ المناسبة.

#### التنفيذ
1. بناء شاشات العناوين
2. إضافة أكثر من عنوان
3. دعم الموقع الحالي
4. ربط العناوين بالمناطق
5. بناء شاشة عرض المطابخ
6. بناء صفحة تفاصيل المطبخ

#### الناتج
- العميل يرى المطابخ المناسبة حسب العنوان أو الموقع

---

### المرحلة 7: السلة وإنشاء الطلب
#### الهدف
تمكين العميل من إرسال طلب حقيقي.

#### التنفيذ
1. بناء السلة
2. دعم الكمية والحجم والملاحظات
3. بناء شاشة مراجعة الطلب
4. اختيار استلام أو توصيل
5. إرسال الطلب
6. تخزين الطلب بحالة `pendingKitchenApproval`

#### الناتج
- طلب جديد يصل إلى المطبخ

---

### المرحلة 8: قرارات المطبخ والرسوم
#### الهدف
تمكين المطبخ من قبول أو رفض الطلب وتحديد رسوم التوصيل.

#### التنفيذ
1. شاشة الطلبات الجديدة
2. شاشة تفاصيل الطلب
3. زر قبول أو رفض
4. إدخال رسوم التوصيل للطلبات التي تحتاج توصيلًا
5. تحديث الطلب إلى `acceptedAwaitingDeposit`

#### الناتج
- الطلب يصبح جاهزًا لمرحلة العربون

---

### المرحلة 9: العربون وإثبات الدفع
#### الهدف
تنفيذ أهم مرحلة مالية في النسخة الأولى.

#### التنفيذ
1. شاشة بيانات العربون للعميل
2. رفع صورة التحويل
3. حفظ الإثبات
4. شاشة مراجعة الإثبات للمطبخ
5. قبول أو رفض الإثبات

#### الناتج
- الطلب ينتقل إلى `depositConfirmed` بعد نجاح المراجعة

---

### المرحلة 10: المحادثة
#### الهدف
فتح التواصل بين الطرفين داخل الطلب.

#### التنفيذ
1. بناء شاشة المحادثة
2. دعم الرسائل النصية
3. دعم الصور
4. ربط المحادثة بالطلب فقط
5. تفعيلها بعد القبول فقط

#### الناتج
- قناة تواصل فعالة بين العميل والمطبخ

---

### المرحلة 11: تتبع الحالة والتسليم
#### الهدف
إظهار التقدم الفعلي للطلب حتى اكتماله.

#### التنفيذ
1. شاشة حالة الطلب
2. الخط الزمني للحالات
3. تحديثات المطبخ:
   - `preparing`
   - `readyForPickup`
   - `awaitingCustomerArrival`
   - `outForDelivery`
   - `deliveredByKitchenOrDriver`
4. زر تأكيد الاستلام للعميل
5. إغلاق الطلب والمحادثة

#### الناتج
- دورة طلب مكتملة من الإنشاء حتى التسليم

---

### المرحلة 12: التقييمات
#### الهدف
إضافة عنصر الثقة والجودة بعد اكتمال الطلب.

#### التنفيذ
1. شاشة تقييم العميل
2. تخزين التقييم
3. إظهار متوسط التقييم داخل صفحة المطبخ
4. إظهار التعليقات

#### الناتج
- نظام تقييم فعال

---

### المرحلة 13: لوحة الإدارة الكاملة
#### الهدف
إعطاء الإدارة السيطرة التشغيلية.

#### التنفيذ
1. اعتماد المطابخ
2. إدارة المناطق
3. إدارة الطلبات
4. إدارة المستخدمين
5. مراجعة التقييمات
6. التدخل في الحالات التشغيلية

#### الناتج
- منصة تشغيل داخلية كاملة

---

### المرحلة 14: الاختبار والتحسين
#### الهدف
تجهيز المنتج للاستخدام التجريبي الواقعي.

#### التنفيذ
1. اختبار رحلة العميل كاملة
2. اختبار رحلة المطبخ كاملة
3. اختبار الإدارة
4. إصلاح الأعطال
5. تحسين الأداء
6. تحسين وضوح الرسائل والحالات

#### الناتج
- نسخة MVP مستقرة

---

## 4. أول رحلة يجب أن تعمل مبكرًا
أفضل هدف تقني مبكر هو تشغيل الرحلة التالية من البداية للنهاية:

1. مطبخ معتمد
2. منيو منشور
3. عميل يختار عنوانًا
4. عميل ينشئ طلبًا
5. مطبخ يقبل الطلب
6. عميل يرفع إثبات العربون
7. مطبخ يؤكد العربون
8. المطبخ يحدث الحالة
9. العميل يؤكد الاستلام

هذا هو `Happy Path` الأساسي للمشروع.

---

## 5. أولويات التنفيذ داخل الكود

### الأولوية القصوى
- قاعدة البيانات
- الصلاحيات
- الطلبات
- العربون
- المحادثة

### أولوية عالية
- المنيو
- العناوين
- اكتشاف المطابخ
- لوحة الاعتماد

### أولوية متوسطة
- التقييمات
- تحسينات UX
- تقارير الإدارة

---

## 6. مخاطر يجب الانتباه لها أثناء التنفيذ
- تعقيد صلاحيات الوصول للملفات الحساسة
- ضبط حالات الطلب بشكل صحيح
- عدم السماح بتجاوز سقف العربون
- وضوح تجربة العميل أثناء دفع العربون
- مزامنة الرسائل والحالات لحظيًا

---

## 7. نقطة بدء التنفيذ الفعلي
عند الانتقال للتنفيذ، أنصح أن نبدأ بهذا الترتيب العملي المباشر:

1. إنشاء هيكل المشاريع
2. إعداد `Supabase`
3. إنشاء الجداول
4. بناء `Auth`
5. بناء `Kitchen Onboarding`
6. بناء `Menu`
7. بناء `Addresses + Discovery`
8. بناء `Orders`
9. بناء `Deposit`
10. بناء `Chat`

---

## 8. الخطوة التالية بعد اكتمال التخطيط
بعد هذه الوثيقة، نكون جاهزين لبدء التنفيذ الفعلي.  
أول تنفيذ عملي ينبغي أن يكون:

- إنشاء هيكل المشروع التقني
- إعداد البيئة
- تثبيت التقنيات
- بدء بناء المرحلة الأولى
